"use strict";
const favTransportation = ["Motorcycle", "Car", "Toyota", "Aeroplane"];
for (let i = 0; i < favTransportation.length; i++) {
    console.log(`I would like to own a ${favTransportation[i]}`);
}
